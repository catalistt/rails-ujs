<tr id="post-row-<% post.id %>">
<td><%= post.title %></td>
<td><%= post.description %></td>
<td><%= post.status %></td>
<td><%= link_to 'Show', post, remote: true %></td>
<td><%= link_to 'Edit', edit_post_path(post), remote: true %></td>
<td><%= link_to 'Destroy', post, method: :delete, remote: true, data: { confirm: '¿Estas segura/o de eliminar el registro?' } %></td>
</tr>
<tr id="post-<%= post.id %>">
</tr>